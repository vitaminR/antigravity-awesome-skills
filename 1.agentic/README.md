# AntiGravity — Agent Guide

This folder is a lightweight agent guide for working in the `AntiGravity` repo.

## Quick rules

- Prefer safe-by-default behavior (read-only/dry-run).
- Update `CATALOG.md` / `skills_index.json` when adding skills.
